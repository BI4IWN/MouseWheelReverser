# Changelog

## 1.1.1 — 2026-09-18

### 修复：重启后手势键失效

- **根因**：蓝牙鼠标重启后的重连速度慢于守护进程启动，原先启动后 0.5 秒的
  一次性型号检测会漏掉 MX Master，导致手势键未启用（滚轮反转与侧键不受影响）
- **修复**：改为事件驱动的持续检测——注册 HID 设备接入回调，鼠标任何时候
  （重启、唤醒、重新配对后）接入都会自动识别并启用手势键

### 其他加固

- osascript（System Events）触发失败时生产模式也会写日志，不再静默
- 修复安装脚本 bootout → bootstrap 竞态偶发导致重装后服务未注册的问题

## 1.1.0 — 2026-08-31

### 新增：按钮与手势功能

- **手势键**（MX Master 系列自动检测启用）：按住并移动鼠标——左/右=切换桌面空间，
  上=任务控制，下=应用窗口；可选单击动作（`--gesture-click=mission`）
- **前进/后退侧键**：拦截并合成为 `Cmd+[` / `Cmd+]`，全应用通用（`--nav=native` 可切回原生）
- **鼠标型号自动适配**：枚举 HID 设备识别型号，按型号启用对应功能（`--list-mice` 查看）

### macOS 26 兼容性新发现

- 系统级快捷键（任务控制/切换空间）对合成键盘事件一律无效（应用级如 `Cmd+[` 正常），
  规避方案：任务控制直接调用系统自带的 Mission Control.app；
  切换空间/应用窗口通过 System Events 特权通道发送

## 1.0.0 — 2026-08-24

首个正式版本。

### 核心

- 只反转鼠标滚轮方向，触摸板手势不受影响（HID 层 CGEventTap + IOHID 物理滚轮监测）
- 适配 macOS 26 (Tahoe)：HID 层拦截 + 只反转 DeltaAxis1，绕过新系统平滑滚动引擎
  对会话层事件修改的忽略（会话层方案在 26 上已无效）
- 支持 MX Master 等高分辨率/平滑滚轮的零值传感器噪声过滤
- 自愈：事件监听被系统静默失效时，依据 HID 旁证自动重建
- 三种反转模式：`delta`（默认）/ `fields` / `repost`

### 工程化

- `install.sh` / `uninstall.sh`：一键安装为后台守护进程 + 开机自启
- 本地固定代码签名证书：升级重编译后无需重新授权
- 授权缺失时自动重试，授权后 10 秒内自愈

